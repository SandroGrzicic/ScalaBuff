package resources.generated;

enum ComputerPeripherals {
	MOUSE = 1;
	KEYBOARD = 2;
}

message Outer {
	enum Inner {
		FIRST = 1;
		SECOND = 2;
	}
	required Inner inner_required = 1;
	optional Inner inner_optional = 2;
	repeated Inner inner_repeated = 3;
}
